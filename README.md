neo mux

intent/ideas:

tmux -CC : iterm2 is nmux : (lib)ghostty
snapshotting
designed for multiple frontends, connected over network connections with packet loss or disconnects (eg mobile)
multi-player
sandbox-friendly (mux sandboxes, mux ptys inside sandboxes)
flatbuffers
session -> tab -> pane
scriptable? plugins?
plugins: adapters - cmux, tmux. ghostty(?)
what would tmux CC mode but specifically for ghostty look like?
